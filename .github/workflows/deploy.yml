# name: CI/CD Pipeline for Easy Com API Deployment (Staging Only)

# on:
#   push:
#     branches: [ stage ]

# jobs:
#   build:
#     runs-on: ubuntu-latest
#     steps:
#     - name: Checkout code
#       uses: actions/checkout@v4

#     - name: Set up Go
#       uses: actions/setup-go@v5
#       with:
#         go-version: '1.25'

#     - name: Build Go binary
#       run: |
#         GOOS=linux GOARCH=amd64 go build -ldflags="-s -w" -o easy_com_api ./cmd/main.go
#       env:
#         CGO_ENABLED: 0  # For static binary

#     - name: Upload binary artifact
#       uses: actions/upload-artifact@v4
#       with:
#         name: deployment-binary
#         path: easy_com_api  # No .env needed (embedded)

#   deploy:
#     needs: build
#     runs-on: ubuntu-latest
#     steps:
#     - name: Checkout code
#       uses: actions/checkout@v4

#     - name: Download binary artifact
#       uses: actions/download-artifact@v4
#       with:
#         name: deployment-binary
#         path: .

#     - name: Install sshpass
#       run: sudo apt update && sudo apt install -y sshpass

#     - name: Create deployment script
#       run: |
#         cat > deploy.sh << 'EOF'
#         #!/bin/bash
#         set -e  # Exit on any error
#         SERVICE="easy_com_api_stage"
#         OPT_DIR="/opt/easy_com_api_stage"

#         echo "Deploying to staging with dir $OPT_DIR"

#         # Stop the running service
#         sudo systemctl stop $SERVICE || true

#         # Backup old binary if exists
#         [ -f $OPT_DIR/easy_com_api ] && sudo mv $OPT_DIR/easy_com_api $OPT_DIR/easy_com_api.backup

#         # Ensure directory exists
#         sudo mkdir -p $OPT_DIR

#         # Move new binary to path
#         sudo mv /tmp/easy_com_api $OPT_DIR/easy_com_api
#         sudo chmod +x $OPT_DIR/easy_com_api

#         # Optional: Run database migrations
#         # cd $OPT_DIR && $OPT_DIR/easy_com_api migrate up

#         # Restart the service
#         sudo systemctl restart $SERVICE
#         sudo systemctl status $SERVICE --no-pager -l

#         # Clean up
#         rm /tmp/easy_com_api

#         # Log deployment
#         echo "Deployed to staging on $(date)"
#         EOF
#         chmod +x deploy.sh

#     - name: Deploy via SSH only
#       env:
#         HOST: ${{ secrets.STAGE_SERVER_HOST }}
#         USERNAME: ${{ secrets.STAGE_SERVER_USERNAME }}
#         PASSWORD: ${{ secrets.STAGE_SERVER_PASSWORD }}
#       run: |
#         # Upload binary via SSH pipe
#         sshpass -p "$PASSWORD" ssh -o StrictHostKeyChecking=no "$USERNAME@$HOST" 'cat > /tmp/easy_com_api' < easy_com_api

#         # Upload deployment script via SSH pipe
#         sshpass -p "$PASSWORD" ssh -o StrictHostKeyChecking=no "$USERNAME@$HOST" 'cat > /tmp/deploy.sh' < deploy.sh

#         # Execute deployment script
#         sshpass -p "$PASSWORD" ssh -o StrictHostKeyChecking=no "$USERNAME@$HOST" "chmod +x /tmp/deploy.sh && /tmp/deploy.sh && rm /tmp/deploy.sh"